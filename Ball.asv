function res = Ball()
    in_play = true;
    ball_radius = 2.86; %cm
    ball_mass = 0.170; %kg
    v_theta = ;
    v_d = -0.5 * 1.28 * v_d^2 ;
    pos_x = cos(v_theta) * v_d;
    pos_y = sin(v_theta) * v_d;
    
    if (in_play)  
        checkTableCollision();
        checkCollision();
    end
end